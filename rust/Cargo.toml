[package]
name = "muse_bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
flutter_rust_bridge = "2.13.0"
serde = { version = "1", features = ["derive"] }
